CREATE TABLE TaskRequests(
    task TEXT NOT NULL,
    id TEXT NOT NULL,
    input JSONB NOT NULL,
    PRIMARY KEY (task, id)
);

CREATE TABLE TaskResults(
    task TEXT NOT NULL,
    id TEXT NOT NULL,
    output JSONB,
    PRIMARY KEY (task, id)
);
